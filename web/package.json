{
  "scripts": {
    "start": "yarn build && next",
    "build": "next build",
    "dev": "next dev"
  },
  "dependencies": {
    "husky": "^1.1.3",
    "lint-staged": "^8.0.4",
    "next": "^7.0.2",
    "prettier": "^1.15.1",
    "react": "^16.6.1",
    "react-dom": "^16.6.1",
    "urql": "^0.2.2"
  },
  "lint-staged": {
    "*.js": [
      "prettier --write --no-semi --single-quote",
      "git add ."
    ]
  }
}
